{
  "name": "nba-yahoo-scraper",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "postCreateCommand": "python -m pip install -U pip && python -m pip install -e . && if [ -f package.json ]; then npm ci; fi && if [ -f package.json ]; then npx playwright install --with-deps chromium; fi",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.analysis.typeCheckingMode": "basic"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}
